#!/usr/bin/env bash

### NOTE TO SELF: ONLY EDIT FROM ~/ ###

# fancy ls
alias lst="ls -FGhl1"

# i always forget what the printer command is...
alias printer="lpr"

# always color grep
alias grep="grep --color"

# reload bash profile and stuff
alias reload="source ~/.bash_profile"

# shorten the most used things
alias l="less"
alias n="nano"
alias g="git"
alias a="atom"

# easier way to cd back one level
alias ..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."

# shorter version for brew cask
alias cask="brew cask"

# hub for git
alias git=hub

# some pandoc settings I always use
alias pd="pandoc -V lang=nl --number-sections --filter pandoc-fignos"

# I like to study using pomodoro technique
# this is a countdown timer with notification
# to time 25 minutes
alias pomodoro="termdown 25m && notify --title=Timer klaar!"
